@page "/Vehiculos"
@inject NavigationManager Navegador;
@inject IJSRuntime JsRuntime;
@inject AgregarVehiculoUseCase AgregarVehiculoUseCase
@inject ListarVehiculosUseCase ListarVehiculosUseCase
@inject EliminarVehiculoUseCase EliminarVehiculoUseCase
@inject ObtenerVehiculosUseCase ObtenerVehiculosUseCase
 
@*@using Aseguradora.UI.Shared;  @*PREGUNTAR PORQUE ME TIRA EL DIALOGO WARNING*@

<PageTitle>Vehiculos</PageTitle>

<h2> Vehiculos </h2>
<div style="margin: 25px; margin-left: 0;">
    <span>Agregar Vehiculo</span>

    <div>
        <input type="text"      placeholder="Dominio"       @bind="v.Dominio" style="max-width: 10%;" />
        <input type="text"      placeholder="Marca"       @bind="v.Marca" style="max-width: 10%;" />
        <input type="number"    placeholder="Anio"        @bind="v.Anio" style="max-width: 10%;" />    
        <input type="number"    placeholder="TitularId"   @bind="v.TitularId" style="max-width: 10%;" />
        <button @onclick="Agregar">Agregar</button>    
    </div>
</div>


@if (vehiculos == null)
{
    <p><em>Cargando vehiculos...</em></p>
}
else
{
    
    <span>Listado de Vehiculoes</span>
    <DialogoConfirmacion @ref=dialogo />
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dominio</th>
                <th>Marca</th>
                <th>Anio</th>
                <th>TitularId</th>
                <th>Accion</th>              
            </tr>
        </thead>
        <tbody>
            @foreach (var v in vehiculos)
            {
                <tr>
                    <td>@v.Id</td>
                    <td>@v.Dominio</td>
                    <td>@v.Marca</td>
                    <td>@v.Anio</td>
                    <td>@v.TitularId</td>       
                    <td>
                        <button class="btn btn-primary">
                            <span class="oi oi-pencil" @onclick="()=>ModificarVehiculo(v)"></span>
                        </button>
                        <button class="btn btn-danger" @onclick="()=>ConfirmarEliminacion(v)" >
                            <span class="oi oi-trash"></span>
                        </button>
                        
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@code 
{

    List<Vehiculo> vehiculos = new List<Vehiculo>();
    protected override void OnInitialized()
    {
        vehiculos = ListarVehiculosUseCase.Ejecutar();
    }
    Vehiculo v = new Vehiculo();

    void Agregar()
    {
        //que pasa si agrego una id de titular que no existe.
        if (v.Dominio != null && v.Marca != null && v.Anio != null && v.TitularId != null )
        {
            Console.WriteLine(v.Dominio + "1");
            if (v.Dominio != null){
                var a = ObtenerVehiculosUseCase.Ejecutar(v.Dominio);
                Console.WriteLine(v.Dominio + "2");
             
                if (a != null)
                {            
                    Console.WriteLine(v.Dominio + "3");
                    AgregarVehiculoUseCase.Ejecutar(v);       
                    Console.WriteLine(v.Dominio + "4");     
                    vehiculos = ListarVehiculosUseCase.Ejecutar();
                }///else
                        ///ya existe ese flaco
            }   
        }
        //else
            //cartel debe completar datos
    }




      void ModificarVehiculo(Vehiculo v)
    {
        Navegador.NavigateTo($"Vehiculo/{v.Dominio}");
    }
    DialogoConfirmacion dialogo = null!;    
    private void ConfirmarEliminacion(Vehiculo Vehiculo)
    {        
        dialogo.Mensaje = $"¿Desea eliminar al cliente {Vehiculo.Marca} {Vehiculo.Dominio}?";
        dialogo.OnConfirmado = EventCallback.Factory.Create(this, () => Eliminar(Vehiculo));   ///ME TIRA UN DIALOGO WARNING AUNQ ESTO ES LO QUE HACE LA TEORIA
        dialogo.Mostrar();
    }
    private void Eliminar(Vehiculo v)
    {

        EliminarVehiculoUseCase.Ejecutar(v.Id);
        vehiculos = ListarVehiculosUseCase.Ejecutar(); //se actualiza la lista de clientes

    }


}